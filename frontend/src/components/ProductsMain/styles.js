import styled from 'styled-components';

export const Container = styled.main`
  padding: 40px;

  .btn-container {
    background-color: inherit;
    button {
      margin: 0 auto;
      width: 100%;
    }
  }
`;