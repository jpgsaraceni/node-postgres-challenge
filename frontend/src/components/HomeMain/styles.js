import styled from 'styled-components';

export const Container = styled.main`
    margin: 5vh 5vw;
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    height: 80vh;
    overflow: scroll;
    overflow-x: hidden;
`;
