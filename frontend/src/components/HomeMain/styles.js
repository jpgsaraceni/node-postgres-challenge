import styled from 'styled-components';

export const Container = styled.div`
    padding: 5vh 0;
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    height: 80vh;
    overflow: scroll;
    overflow-x: hidden;
`;
